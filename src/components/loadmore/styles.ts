import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const StyledLoadMore = styled(Box)({
   position: 'absolute',
   left: '50%',
   transform: 'translateX(-50%)',
   overflow: 'hidden'
})